/**
 * Shared local ADT proxy registry and loopback HTTP client for fetch reuse.
 * See {@link org.openadt.product.proxy.LocalProxyRegistry} and {@code specs/proxy.md}.
 */
package org.openadt.product.proxy;
