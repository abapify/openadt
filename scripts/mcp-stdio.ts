#!/usr/bin/env bun
/**
 * Repo-root stdio MCP entry for IDE / agent MCP config.
 * Invoked as: bun scripts/mcp-stdio.ts  (or nx run sap-adt-mcp-launcher:serve-stdio)
 */
import "../tools/sap-adt-mcp-launcher/src/mcp-stdio-entry.ts";
