# Codacy Guardrails — disable noisy rules (see docs.codacy.com/codacy-guardrails/.../configure-rules).
# TOML config keys such as http_truststore_password are not secrets; hard-coded-password is a false positive.
rules:
  - id: hardcoded-password
    severity: OFF
