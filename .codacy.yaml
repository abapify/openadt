---
# Repository Codacy configuration (see https://docs.codacy.com/repositories-configure/codacy-configuration-file/)
#
# Guardrails (IDE/MCP): .codacy/guardrails.yaml — hardcoded-password OFF (TOML key names, not secrets).
# Opengrep/Semgrep: use root .semgrep.yml — do not exclude production Java files here.
# Loopback SSRF false-positive context for AI review: .codacy/instructions/review.md
